import {FC} from 'react';
import User from "@features/user/components";

const UserPage: FC = () => {
    return (
        <main>
            <User/>
        </main>
    );
}

export default UserPage;